name: Public safety check

on:
  push:
  pull_request:

jobs:
  public-safety:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Run public safety scan
        run: bash scripts/public_safety_check.sh
